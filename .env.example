# Where the local SQLite database file lives. Defaults to ./data/open-pantry.db
# relative to the project root. Point this at any writable path you like
# (e.g. a Docker volume mount).
# OPEN_PANTRY_DB_PATH=./data/open-pantry.db
